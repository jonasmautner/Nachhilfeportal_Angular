import { LearningofferFactory } from './learningoffer.factory';

describe('LearningofferFactory', () => {
  it('should create an instance', () => {
    expect(new LearningofferFactory()).toBeTruthy();
  });
});
