import { LearningofferObservable } from './learningoffer-observable';

describe('LearningofferObservable', () => {
  it('should create an instance', () => {
    expect(new LearningofferObservable()).toBeTruthy();
  });
});
